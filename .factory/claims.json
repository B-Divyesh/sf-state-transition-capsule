[
  {
    "id": "first-divergence",
    "claim": "The demo identifies the first changed state field between two recorded runs.",
    "where": "landing page, demo, README",
    "test": "npm run test:e2e -- --grep @claim:first-divergence",
    "sandbox": "fresh browser context at /demo with bundled sample capsules; assert the event and exact $.report.chart path"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo sample data is separate and is not saved to real user storage.",
    "where": "demo banner",
    "test": "npm run test:e2e -- --grep @claim:demo-isolation",
    "sandbox": "seed a real-data marker, enter /demo, reset, and prove the real marker is unchanged and no demo keys remain"
  },
  {
    "id": "local-processing",
    "claim": "Capsule comparison stays in the browser and does not upload capsule data.",
    "where": "landing facts, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:local-processing",
    "sandbox": "record every request during the complete /demo comparison and assert that every request is a same-origin GET"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "landing facts, README",
    "test": "npm run build && npm run test:e2e:production -- --grep @claim:offline-reload",
    "sandbox": "fresh browser-owned context; await service-worker activation and control, go offline, reload /demo, and assert the comparison result"
  },
  {
    "id": "redaction",
    "claim": "Configured fields are redacted before state, event, and metadata values enter a capsule.",
    "where": "landing page, README",
    "test": "npm run test:unit -- --testNamePattern @claim:redaction",
    "sandbox": "record known secrets with exact and wildcard paths; assert capsule output is redacted and source input is unchanged"
  },
  {
    "id": "pure-replay",
    "claim": "Replay runs only the reducer supplied by the application and reports the first divergence.",
    "where": "landing page, README",
    "test": "npm run test:unit -- --testNamePattern @claim:pure-replay",
    "sandbox": "replay a bundled capsule through a supplied pure reducer and assert matching and divergent results"
  },
  {
    "id": "package-formats",
    "claim": "The package ships working ESM, CommonJS, and TypeScript declarations with no runtime dependencies.",
    "where": "API section, README",
    "test": "npm run build && npm run test:artifacts -- --testNamePattern @claim:package-formats",
    "sandbox": "build from source, load ESM and CommonJS outputs, inspect declarations, and assert no runtime dependency manifest"
  },
  {
    "id": "license-restore",
    "claim": "A valid Studio license enables local history without gating the comparison workbench.",
    "where": "Studio section, terms, README",
    "test": "npm run test:e2e -- --grep @claim:license-restore",
    "sandbox": "mock the documented Sociobot verification response, restore a token, and assert Studio history and the free comparison control"
  },
  {
    "id": "tab-local-storage",
    "claim": "Imported capsules stay in the current tab unless Studio local retention is enabled.",
    "where": "landing facts, workbench, privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:tab-local-storage",
    "sandbox": "fresh browser context; import two valid capsules without a license, assert no storage keys, reload, and assert both import bays are empty"
  },
  {
    "id": "no-telemetry-runtime",
    "claim": "Without a license action, standard and demo visits make no telemetry, API, or third-party runtime request.",
    "where": "privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:no-telemetry-runtime",
    "sandbox": "fresh browser context with no license; compare bundled sample on / and /demo while recording requests, allowing only same-origin GET navigations and static assets"
  },
  {
    "id": "studio-price",
    "claim": "Capsule Studio costs $39 as a one-time purchase per user.",
    "where": "landing facts, Studio section, terms, README",
    "test": "npm run test:e2e -- --grep @claim:studio-price",
    "sandbox": "fresh browser context; assert the landing price, exact hosted checkout URL, and the matching terms disclosure"
  }
]
